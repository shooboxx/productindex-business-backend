import db from '../../models'



const savePortfolioUrl = async(url) => {

}

const saveProductUrl = async(url) => {
    
}

const saveProfileUrl = async(url) => {
    
}

export const StorageRepo = {
    savePortfolioUrl,
    saveProductUrl,
    saveProfileUrl
}