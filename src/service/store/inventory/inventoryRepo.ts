import { CreateInventoryItem, InventoryItem } from "./inventoryTypes";

const addInventoryItems = async (inventoryItem : CreateInventoryItem[]) : Promise<InventoryItem[]>=> {
    return
}


export const InventoryRepo = {
    addInventoryItems
}